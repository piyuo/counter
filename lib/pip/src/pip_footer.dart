import 'package:flutter/cupertino.dart';

/// A footer provide extra padding to let user ease to tap button on the bottom
class PipFooter extends StatelessWidget {
  const PipFooter({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 52);
  }
}
