class MonitorRoutes {
  const MonitorRoutes._();
  static const String root = '/'; // nothing to show, e.g. no cameras connected, just a empty screen with background.
  static const String video = '/video'; // vision preview
}
